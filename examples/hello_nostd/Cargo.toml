[package]
name = "hello_nostd"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
rico8 = { path = "../../rico8", default-features = false }
heapless = "0.8"

[workspace]

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
